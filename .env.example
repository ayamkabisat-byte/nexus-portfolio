# The final public URL. Replace this when a custom domain is connected.
SITE_URL=https://michaeldinko.vercel.app

# Keep / for Vercel, Netlify, Cloudflare Pages, or a custom domain.
# For a GitHub Pages project URL, use /nexus-portfolio/.
BASE_PATH=/

# Optional: paste the verification token from Google Search Console.
GOOGLE_SITE_VERIFICATION=
